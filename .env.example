# Скопіюйте у .env (або задайте як змінну середовища ОС) і заповніть.
# .env у git НЕ потрапляє (.gitignore).

# Потрібен лише для діаризації (розрізнення спікерів) на ASR-шляху.
# Отримати: https://huggingface.co/settings/tokens
# + прийняти ліцензію: https://huggingface.co/pyannote/speaker-diarization-community-1
# Без токена ASR працює, але без міток [Спікер N].
HF_TOKEN=
